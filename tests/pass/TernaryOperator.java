public class TernaryOperator {
    public void ternary() {
        int x = 4;
        int y = 3;
        int z = 2;
        boolean r = true;
        int u = x > 2 ? 5 : 4;
        int k = x > (z - x + x) ? r ? 1 : 5 + 7 : 2 + 3;
        (k + 1) += 4;
    }
}
